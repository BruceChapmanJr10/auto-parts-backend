spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/autoparts
    username: postgres
    password: yourpassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080